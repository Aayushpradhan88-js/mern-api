import {useEffect, useRef, useState} from 'react'

export const Logic = () => {

  const ws = useRef(null)
  const [message, setMessage] = useState('')
  const []
  return (
    <div>Logic</div>
  )
}
